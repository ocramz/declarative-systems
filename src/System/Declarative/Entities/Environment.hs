module System.Declarative.Entities.Environment where


-- | EXAMPLE QUERIES
-- observe : does the current environment contain a variable X ?
-- assert : does an env.var. X (if it exists) have value Y ?

